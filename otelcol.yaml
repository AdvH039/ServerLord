receivers:
  otlp:
    protocols:
      http:
        endpoint: nodejs-metrics-demo-collector:4318

processors:
  batch:

exporters:
  otlphttp:
    endpoint: https://in-otel.logs.betterstack.com/metrics
    headers:
      Authorization: "Bearer vJUo8L87mmENSyX5zR84tkVL"

service:
  pipelines:
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlphttp]
